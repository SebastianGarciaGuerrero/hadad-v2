# Copia este archivo como .env y completa los valores reales.
# .env NO se sube a Git (está en .gitignore).

POSTGRES_USER=hadad_admin
POSTGRES_PASSWORD=cambiar_por_password_seguro
POSTGRES_DB=hadad_v2
